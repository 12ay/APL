project2.cpp contains the main function and instantiates the btc class.
project2.hxx contains the class declaration.

To compile 'project2.cpp', first make sure that 'project2.cpp' and 'project2.hxx'
are in the same directory/repository. Then, use the command:
g++ -O2 -o Project2 project2.cpp